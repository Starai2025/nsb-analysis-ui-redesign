# LA DOTD Demo Agent Scaffold

These briefs are lightweight starting points for specialist Claude subagents working on the `demo/ladot-calcasieu` branch.

Each agent must follow `../../CLAUDE.md` and preserve:

- local-first persistence
- the existing route shell
- the current intake -> summary -> report -> draft -> sources flow
- compatibility with the current browser thread model

Suggested streams:

- `architecture-persistence.md`
- `intake-redesign.md`
- `issue-correspondence-model.md`
- `output-compatibility.md`
- `qa-regression.md`
