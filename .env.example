# ANTHROPIC_API_KEY: Required for Claude AI API calls (server-side only).
# Never expose this to the browser.
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# PORT: Optional. Defaults to 3000.
# PORT=3000
